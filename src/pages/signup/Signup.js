import Connexion from '../../components/connexion/Connexion';

const Signup = ({location}) => {
  
  return ( 
    <section>
      <Connexion location={location.pathname} />
    </section>
    
  )
}

export default Signup