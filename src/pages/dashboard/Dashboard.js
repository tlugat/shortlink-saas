import styles from './Dashboard.module.scss'

const Dashboard = () => {
  return ( 
    <section className={styles.dashboard}>
      <div><span>Prochainement !</span></div>
    </section>
  )
}

export default Dashboard